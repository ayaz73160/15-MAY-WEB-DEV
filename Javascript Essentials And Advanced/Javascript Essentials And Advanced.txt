MODULE 4 (JavaScript Basic & DOM)

Q.1 What is JavaScript. How to use it?
- JavaScript is a client-side scripting language used to make web pages interactive and dynamic.
- It can be used inside <script> tags or with external .js files.

Q.2 How many types of Variable in JavaScript?
- var (function scoped)
- let (block scoped, introduced in ES6)
- const (block scoped, cannot be reassigned)

Q.3 Define Data Types in JS?
- Primitive types: String, Number, Boolean, Null, Undefined, Symbol, BigInt
- Non-primitive types: Object, Array, Function

Q.4 Write a mul Function Which will Work Properly When invoked With Following Syntax.
function mul(a) {
  return function(b) {
    return a * b;
  }
}
console.log(mul(2)(3)); // 6

Q.5 Difference between undefined and undeclared in JavaScript?
- undefined: Variable is declared but no value assigned.
- undeclared: Variable is not declared at all in the program.

Q.6 Using console.log() print out the following statement:
console.log("The quote 'There is no exercise better for the heart than reaching down and lifting people up.' by John Holmes teaches us to help one another.");
console.log("Mother Teresa said: 'If you judge people, you have no time to love them.'");

Q.7 Check if typeof '10' is exactly equal to 10. If not make it exactly equal?
console.log(typeof '10' === 10); // false
console.log(Number('10') === 10); // true

Q.8 Write a JavaScript Program to find the area of a triangle?
let base = 10;
let height = 5;
let area = (base * height) / 2;
console.log("Area of triangle: " + area);

Q.9 Write a JavaScript program to calculate days left until next Christmas?
let today = new Date();
let xmas = new Date(today.getFullYear(), 11, 25);
if (today > xmas) {
  xmas.setFullYear(xmas.getFullYear() + 1);
}
let diff = Math.ceil((xmas - today) / (1000 * 60 * 60 * 24));
console.log(diff + " days left until Christmas");

Q.10 What is Condition Statement?
- Conditional statements are used to perform actions based on conditions.
- Examples: if, else if, else, switch.

Q.11 Find circumference of Rectangle formula : C = 4 * a ?
- Actually C = 2 * (l + w)
- If square: C = 4 * a

Q.12 WAP to convert years into days and days into years?
let years = 2;
let days = years * 365;
console.log(days + " days");

let totalDays = 730;
let convYears = totalDays / 365;
console.log(convYears + " years");

Q.13 Convert temperature Fahrenheit to Celsius?
let f = 100;
let c = (f - 32) * 5/9;
console.log("Celsius: " + c);

Q.14 Write a JavaScript exercise to get the extension of a filename?
let filename = "script.js";
let ext = filename.split('.').pop();
console.log("Extension: " + ext);

Q.15 What is the result of the expression (5 > 3 && 2 < 4)?
// true

Q.16 What is the result of the expression (true && 1 && "hello")?
// "hello"

Q.17 What is the result of the expression true && false || false && true?
// false

Q.18 What is a Loop and Switch Case in JavaScript define that?
- Loop: Executes code repeatedly (for, while, do while, for..in, for..of).
- Switch: Executes one case out of multiple options.

Q.19 What is the use of isNaN function?
- isNaN() checks if a value is Not-a-Number.
console.log(isNaN("hello")); // true

Q.20 What is the difference between && and || in JavaScript?
- && (AND) returns true if both are true.
- || (OR) returns true if any one is true.

Q.21 What is the use of Void (0)?
- Used to prevent browser from loading a new page when using hyperlinks.
<a href="javascript:void(0)">Click</a>

Q.22 Check Number Is Positive or Negative in JavaScript?
let num = -5;
if (num > 0) console.log("Positive");
else console.log("Negative");

Q.23 Find the Character Is Vowel or Not ?
let ch = 'a';
if ("aeiouAEIOU".includes(ch)) console.log("Vowel");
else console.log("Not Vowel");

Q.24 Write to check whether a number is negative, positive or zero?
let n = 0;
if (n > 0) console.log("Positive");
else if (n < 0) console.log("Negative");
else console.log("Zero");

Q.25 Write to find number is even or odd using ternary operator in JS?
let x = 7;
console.log(x % 2 === 0 ? "Even" : "Odd");

Q.26 Write find maximum number among 3 numbers using ternary operator in JS?
let a = 5, b = 9, c = 3;
let max = a > b ? (a > c ? a : c) : (b > c ? b : c);
console.log("Max: " + max);

Q.27 Write to find minimum number among 3 numbers using ternary operator in JS?
let min = a < b ? (a < c ? a : c) : (b < c ? b : c);
console.log("Min: " + min);

Q.28 Write to find the largest of three numbers in JS?
if (a >= b && a >= c) console.log("Largest: " + a);
else if (b >= a && b >= c) console.log("Largest: " + b);
else console.log("Largest: " + c);

Q.29 Write to show
i. Monday to Sunday using switch case in JS?
let day = 3;
switch(day) {
  case 1: console.log("Monday"); break;
  case 2: console.log("Tuesday"); break;
  case 3: console.log("Wednesday"); break;
  case 4: console.log("Thursday"); break;
  case 5: console.log("Friday"); break;
  case 6: console.log("Saturday"); break;
  case 7: console.log("Sunday"); break;
  default: console.log("Invalid");
}

ii. Vowel or Consonant using switch case in JS?
let letter = 'e';
switch(letter) {
  case 'a': case 'e': case 'i': case 'o': case 'u':
  case 'A': case 'E': case 'I': case 'O': case 'U':
    console.log("Vowel");
    break;
  default:
    console.log("Consonant");
}

Q.30 What are the looping structures in JavaScript? Any one Example?
- for, while, do while, for..in, for..of
Example:
for (let i=1; i<=5; i++) {
  console.log(i);
}

Q.31 Write a print 972 to 897 using for loop in JS?
for (let i=972; i>=897; i--) {
  console.log(i);
}

Q.32 Write to print factorial of given number?
let num1 = 5;
let fact = 1;
for (let i=1; i<=num1; i++) fact *= i;
console.log("Factorial: " + fact);

Q.33 Write to print Fibonacci series up to given numbers?
let n1=0, n2=1, next;
for (let i=1; i<=10; i++) {
  console.log(n1);
  next = n1 + n2;
  n1 = n2;
  n2 = next;
}

Q.34 Write to print number in reverse order?
let number = 64728;
let rev = Number(String(number).split("").reverse().join(""));
console.log(rev);

Q.35 Write a program make a summation of given number?
let num2 = 1523;
let sum = 0;
for (let d of String(num2)) sum += Number(d);
console.log(sum);

Q.36 Write a program you have to make a summation of first and last Digit?
let n2 = 1234;
let str = String(n2);
let ans = Number(str[0]) + Number(str[str.length-1]);
console.log(ans);

Q.37 Use console.log() and escape characters to print the following pattern:
console.log("1 1 1 1 1");
console.log("2 1 2 4 8");
console.log("3 1 3 9 27");
console.log("4 1 4 16 64");
console.log("5 1 5 25 125");

Q.38 Patterns (examples shown for one):
1)
for (let i=1; i<=5; i++) {
  let row="";
  for (let j=1; j<=i; j++) {
    row += (j%2==1? "1 ":"0 ");
  }
  console.log(row);
}

(Other patterns can be written similarly)

Q.39 Accept 3 numbers from user using while loop and check each numbers palindrome?
let nums = [121, 234, 454];
let i=0;
while(i<nums.length){
  let str = String(nums[i]);
  let rev = str.split("").reverse().join("");
  console.log(nums[i] + (str===rev ? " is Palindrome" : " not Palindrome"));
  i++;
}

Q.40 Display current day and time?
let today2 = new Date();
let days = ["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
console.log("Today is " + days[today2.getDay()]);
console.log("Current Time is " + today2.getHours() + ":" + today2.getMinutes());

Q.41 Program to get the current date?
let d = new Date();
console.log(d);

Q.42 Program to compare two objects?
function isEqual(obj1, obj2){
  return JSON.stringify(obj1) === JSON.stringify(obj2);
}
console.log(isEqual({a:1},{a:1}));

Q.43 Convert array of objects into CSV string?
let arr = [{name:"John",age:30},{name:"Sam",age:25}];
let csv = arr.map(o=>Object.values(o).join(",")).join("\n");
console.log(csv);

Q.44 Capitalize first letter of a string?
let str2 = "hello";
let result = str2.charAt(0).toUpperCase() + str2.slice(1);
console.log(result);

Q.45 Determine if variable is array?
console.log(Array.isArray([1,2,3]));

Q.46 Clone an array?
let original = [1,2,3];
let copy = [...original];
console.log(copy);

Q.47 Drawback of declaring methods directly in objects?
- Methods get duplicated for every object, increasing memory usage.

Q.48 Print length of string?
let str3 = "Hello World";
console.log(str3.length);

Q.49 Change all string characters to uppercase?
console.log("hello".toUpperCase());

Q.50 Same as Q.47

Q.51 Current date formats?
let date = new Date();
let mm = date.getMonth()+1;
let dd = date.getDate();
let yyyy = date.getFullYear();
console.log(mm+"-"+dd+"-"+yyyy);
console.log(mm+"/"+dd+"/"+yyyy);
console.log(dd+"-"+mm+"-"+yyyy);
console.log(dd+"/"+mm+"/"+yyyy);

Q.52 Use indexOf to determine position of 'a' in "30 Days Of JavaScript"?
console.log("30 Days Of JavaScript".indexOf("a"));

Q.53 Use lastIndexOf to determine position of 'a'?
console.log("30 Days Of JavaScript".lastIndexOf("a"));

Q.54 Form Validation in JS?
- Use JS to check inputs before submitting. Example:
if(document.getElementById("email").value=="") alert("Email required");

Q.55 Form in Email, number, Password Validation?
- Use regex or HTML5 attributes like type="email", type="number", type="password".

Q.56 Dynamic Form Validation?
- Add event listeners on input fields and validate in real time.

Q.57 How many types of JS Event? How to use it?
- Mouse events, Keyboard events, Form events, Window events.
- Use addEventListener or inline attributes like onclick.

Q.59 BOM vs DOM?
- DOM: Document Object Model (HTML structure).
- BOM: Browser Object Model (browser features like window, history).

Q.60 Array vs Object differences?
- Array: Ordered collection, indexed by numbers.
- Object: Unordered collection, indexed by keys.

Q.61 Split string into array using split()?
console.log("Hello World".split(" "));

Q.62 Check if string contains word Script using includes()?
console.log("30 Days Of JavaScript".includes("Script"));

Q.63 Change string to lowercase?
console.log("HELLO".toLowerCase());

Q.64 Character at index 15 in "30 Days of JavaScript"?
console.log("30 Days of JavaScript".charAt(15));

Q.65 Copy one string to another?
let s1 = "Hello";
let s2 = s1;
console.log(s2);

Find length of string without using library?
let s3 = "Hello";
let count=0;
for(let ch of s3){ count++; }
console.log(count);

Other short Qs:
- Negative Infinity: A special number value less than any number.
- Company developed JS: Netscape (Brendan Eich).
- === operator: Strict equality (checks type + value).
- Add element dynamically:
let p = document.createElement("p");
p.textContent="Hello";
document.body.appendChild(p);

Popup boxes:
- alert(), confirm(), prompt()

Force page reload:
location.href="newpage.html";

Disadvantages of innerHTML:
- Replaces entire content, less secure, slower.

Password field with show/hide:
<input type="password" id="pwd">
<button onclick="toggle()">Show</button>
<script>
function toggle(){
 let x=document.getElementById("pwd");
 x.type = (x.type==="password")? "text":"password";
}
</script>

Basic math operations:
console.log(2+3);
console.log(5-2);
console.log(4*3);
console.log(10/2);

---

MODULE 2 (Data Types and Objects)

Create empty object and modify:
let user = {};
user.name="John";
user.surname="Smith";
user.name="Pete";
delete user.name;

Array copied example:
let fruits=["Apples","Pear","Orange"];
let shoppingCart=fruits;
shoppingCart.push("Banana");
alert(fruits.length); // 4

Map to names:
let users=[{name:"John",age:25},{name:"Pete",age:30},{name:"Mary",age:28}];
let names=users.map(u=>u.name);
console.log(names);

Map to objects with fullName:
let users2=[{name:"John",surname:"Smith",id:1},{name:"Pete",surname:"Hunt",id:2},{name:"Mary",surname:"Key",id:3}];
let usersMapped=users2.map(u=>({fullName:u.name+" "+u.surname,id:u.id}));
console.log(usersMapped);

Sum of salaries:
function sumSalaries(salaries){
 let sum=0;
 for (let val of Object.values(salaries)){ sum+=val; }
 return sum;
}
let salaries={John:100,Pete:300,Mary:250};
console.log(sumSalaries(salaries));

Destructuring:
let user2={name:"John",years:30};
let {name,years:age,isAdmin=false}=user2;
console.log(name,age,isAdmin);

Turn object into JSON and back:
let user3={name:"John Smith",age:35};
let json=JSON.stringify(user3);
let obj=JSON.parse(json);

---

MODULE 3 (Document, Event and Controls)

Hide/show password: (done above)
Select all classes and loop:
let elems=document.querySelectorAll(".myclass");
elems.forEach(e=> e.addEventListener("click",()=>alert("Clicked")) );

Responsive header: (basic toggle with JS)
Form validation: check empty inputs before submit.
Modal box: create div with CSS hidden, show with JS.
Slider: use external JS library like Swiper.
Prevent form submit:
form.addEventListener("submit", e=> e.preventDefault());

---

Some Expression Results:
1. 5 > 4 -> true
2. "apple" > "pineapple" -> false
3. "2" > "12" -> true (string comparison)
4. undefined == null -> true
5. undefined === null -> false
6. null == "\n0\n" -> false
7. null === +"\n0\n" -> false

if("0"){alert("Hello");} -> Yes, alert shown (non-empty string is true).

alert(null || 2 || undefined); -> 2

checkAge function:
function checkAge(age){
 if(age>18) return true;
 else return confirm("Did parents allow you?");
}

Arrow functions:
function ask(q,yes,no){
 if(confirm(q)) yes(); else no();
}
ask("Do you agree?", ()=>alert("You agreed."), ()=>alert("You canceled."));

